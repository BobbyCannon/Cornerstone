<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		android:installLocation="auto"
		package="com.bobbycannon.cornerstone.sample">
	<uses-sdk android:minSdkVersion="34"
			android:targetSdkVersion="36" />

	<!-- Camera preview / record (TabCamera). Not required so the app installs on devices without a camera. -->
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-feature android:name="android.hardware.camera"
			android:required="false" />
	<!-- Video recording enables audio via WithAudioEnabled. -->
	<uses-permission android:name="android.permission.RECORD_AUDIO" />
	<!-- Media player remote URL sample. -->
	<uses-permission android:name="android.permission.INTERNET" />

	<application android:label="Sample"
			android:icon="@drawable/Icon">
	</application>
</manifest>